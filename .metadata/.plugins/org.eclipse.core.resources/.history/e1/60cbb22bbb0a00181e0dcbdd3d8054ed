#include <msp430.h> 

/*
 * main.c
 */
int main(void) {
    WDTCTL = WDTPW | WDTHOLD;	// Stop watchdog timer

    int x, y;

    y = 0;

    for(x=0; x<10; x=x+1)
    {
    	y = y + x;
    }

    while(1);
}
